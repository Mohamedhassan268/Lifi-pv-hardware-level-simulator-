/**
 * ReceiverInspector — reuses the existing ReceiverTab form body.
 */

import { ReceiverTab } from "@/features/build/ReceiverTab";

export function ReceiverInspector() {
  return <ReceiverTab />;
}
