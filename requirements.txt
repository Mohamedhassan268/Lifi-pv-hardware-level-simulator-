# Hardware-Faithful LiFi-PV Simulator — Python Dependencies
#
# Quick start:
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   source .venv/bin/activate       # Linux/macOS
#   pip install -r requirements.txt
#
# Optional extras (install separately if needed):
#   pip install -r requirements-optional.txt

# === Core Simulation ===
numpy>=2.0
scipy>=1.12
matplotlib>=3.8

# === GUI ===
PyQt6>=6.6

# === Schematics ===
schemdraw>=0.22

# === FEC (LDPC) ===
# Required by cosim/fec.py for the ieee_802_11bb preset's LDPC 5/6 decoder.
# Lazy-imported, so the rest of the simulator runs without it, but the
# 802.11bb pipeline will error out at decode time if it's missing.
# Install with `pip install pyldpc --no-build-isolation` so pyldpc's setup
# sees the existing numpy (pyldpc==0.7.9 pulls numba + llvmlite).
pyldpc>=0.7.9

# === Other ===
requests>=2.31
PyYAML>=6.0
